PORT=5000
CORS_ORIGIN=http://localhost:3000

# Optional: forward submissions to your own API endpoint
APPOINTMENT_API_URL=
APPOINTMENT_API_METHOD=POST
APPOINTMENT_API_HEADERS_JSON={}

# Optional: send submissions by email via SMTP
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM="Care2 Training <no-reply@care2training.com>"
MAIL_TO=
MAIL_SUBJECT=New appointment request
